<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.atami.mgodroid"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="15" />

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".views.MGoBlogActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".views.NodeActivity"
            android:label="@string/app_name" >
        </activity>

        <provider
            android:name=".provider.NodeIndexProvider"
            android:authorities="com.mgoblog.nodeindexprovider" >
        </provider>
        
        <provider
            android:name=".provider.NodeProvider"
            android:authorities="com.mgoblog.nodeprovider" >
        </provider>
        
        <service android:name=".io.NodeIndexService"/>
        <service android:name=".io.NodeService" />
    </application>

</manifest>